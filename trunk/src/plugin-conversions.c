#include <gtk/gtk.h>

#include "gscore.h"
#include "plugin.h"

struct _Identity Identity;

/* Score_t *plugin_to_gscore(ScorePI *plugin) */
/* { */

/* } */

/* Score_t *gscore_to_plugin(Score_t *score) */
/* { */
/* 	return score; */
/* /\*         ScorePI *spi; *\/ */

/* /\* 	spi = g_malloc(sizeof(ScorePI)); *\/ */

/* /\*         spi->Staff_list = g_list_copy(score->Staff_list); *\/ */

/* /\*         spi->tempo = score->tempo; *\/ */

/* /\*         spi->Identity = g_malloc(sizeof(Identity)); *\/ */
/* /\*         spi->Identity->title = score->Identity->title->str; *\/ */
/* /\*         spi->Identity->subtitle = score->Identity->subtitle->str; *\/ */
/* /\*         spi->Identity->composer = score->Identity->composer->str; *\/ */

/* /\*         return spi; *\/ */
/* } */
